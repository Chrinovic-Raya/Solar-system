Just the relay + two protection components + two capacitors + solar cell (20V). No LCD, no IR yet. Use an LED or serial print to confirm relay state (series vs parallel). Verify with a voltmeter across the capacitors that switching actually changes the topology correctly.

- Tested 